{
  "buildCommand": "npm install --legacy-peer-deps --prefix frontend && npm run --prefix frontend build",
  "outputDirectory": "frontend/build",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/index.py"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}